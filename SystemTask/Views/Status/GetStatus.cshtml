@model IEnumerable<SystemTask.Models.Status>

<p>
    @Html.ActionLink("Create New", "AddStatus")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.idStatus)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.nameStatus)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.idStatus)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.nameStatus)
        </td>
        <td>
            @Html.ActionLink("Edit", "EditStatus", new {  idStatus=item.idStatus  }) |
            @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Delete", "DeleteStatus", new {  idStatus=item.idStatus  }, new { onclick = "return confirm('Are you sure you want to delete this task?');" })
        </td>
    </tr>
}

</table>
